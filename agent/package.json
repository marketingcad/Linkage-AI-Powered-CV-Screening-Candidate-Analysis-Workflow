{
  "name": "screenai-interview-agent",
  "private": true,
  "type": "module",
  "description": "AI voice interviewer worker — joins LiveKit interview rooms and runs a Gemini Live interview.",
  "scripts": {
    "dev": "tsx src/index.ts dev",
    "start": "tsx src/index.ts start",
    "build": "echo \"no build step — the agent runs via tsx\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@livekit/agents": "^1.4.0",
    "@livekit/agents-plugin-google": "^1.4.0",
    "dotenv": "^17.4.2",
    "tsx": "^4.23.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
